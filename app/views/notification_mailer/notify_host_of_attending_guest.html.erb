<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
			<%= @attendee.full_name %> has confirmed that they are coming to your event: <%= @event.name %></p>
			<p><b>Number of additional guests:</b> <%= @attendee.num_of_guests %> </p>
			<p><b>Their comment:</b>  <%= @attendee.comment %></p>
			<p><% unless @attendee.dish.empty? %>
			<span style='font-weight:bold;font-size:1em'><b>Bringing items:</b> </span>
			<div style="inline-block">
				<% @attendee.dish.each do |dish| %>
					<span class="label"><%= dish["category"] %></span>&nbsp;&nbsp;&nbsp;<span style="font-weight:bold"><%= dish["item"] %></span><br />
				<% end %>
			</div>
		<% else %>
			<%= "Bringing nothing" %>
		<% end %>
    </p>
  </body>
</html>

