<%= form_for([@event, @suggestion], :html=>{:id=>"suggestionsForm"}) do |f| %>
  <% if @suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% @suggestion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<p>
		<%= f.text_field :new_or_existing %>
	</p>
	<p>
		<%= f.text_field :category %>
	</p>
	<p>
		<%= f.text_area :suggested_items %>
	</p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
