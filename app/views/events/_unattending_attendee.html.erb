<div class="guest_breakpoint">
	<p><% if unattending_attendee.full_name %> <%= unattending_attendee.full_name %> <% else %> <%= unattending_attendee.email %><% end %><span style="float:right"><%= link_to "Email guest", unattending_attendee.event.id.to_s+'/attendees/'+unattending_attendee.id.to_s+'/email_guest', {:style=>"color:blue", :class => "btn btn-link"} %></span></p>
	<p>Comment(s): <%= unattending_attendee.comment %></p>
</div>

