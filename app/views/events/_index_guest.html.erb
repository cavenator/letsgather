  <tr>
		<div class="event-line">
		<td style="font-size:16px"><%= event.name %>
		<div>
			<%= "Host: #{event.user.full_name}" %>
		</div>
    </td>
		<td style="font-size:16px"><%= "Starts on "+ event.start_date.strftime("%m/%d/%Y") %>
		<div>You are currently <%= Attendee.find_rsvp_for(current_user, event) %></div></td>
    <td><%= link_to 'Show', event %></td>
		<td />
		<td />
		</div>
  </tr>

