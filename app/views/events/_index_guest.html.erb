  <tr>
		<div class="event-line">
		<td style="font-size:16px"><%= event.name %>
		<div>
			<%= "Host: #{event.user.full_name}" %>
		</div>
    </td>
		<td style="font-size:16px"><%= "Starts on "+ event.start_date.in_time_zone("Pacific Time (US & Canada)").strftime("%m/%d/%Y") %>
		<div>You are currently <%= Attendee.find_rsvp_for(current_user, event) %></div></td>
    <td><span class="btn btn-success"><%= link_to 'View', event, { :style => "color:white" } %></span></td>
		<td />
		<td />
		</div>
  </tr>

