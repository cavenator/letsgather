<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
		<% if @host.id == @sender.id %>
    	<h2>A message from your host<%= @host.email %> regarding <%= @event.name %></h2>
		<% else %>
			<h2>A message from <%= @sender.full_name %>(<%= @sender.email %>) </h2>
		<% end %>
    <p>
			<%= @body %>
    </p>
  </body>
</html>

