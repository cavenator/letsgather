require 'spec_helper'

describe AttendeesController do
	describe "GET add_attendees" do
		it "provides an area to add guests" do
#			@event = FactoryGirl.create(:event)
#			get 'add_attendees', :event_id => @event.id, :user_id => @user.id
		end
	end
end
